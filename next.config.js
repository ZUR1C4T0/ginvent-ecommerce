/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'dawid-next-ecommerce.s3.amazonaws.com',
            'placehold.co',
            'api.lorem.space',
        ],
    },
}

module.exports = nextConfig
